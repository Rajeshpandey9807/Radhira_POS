@model IReadOnlyList<StateListItem>
@{
    ViewData["Title"] = "States";
}

<section class="users-header">
    <div>
        <p class="eyebrow">Business setup</p>
        <h2>States</h2>
        <p>Maintain every state where you operate to keep compliance tidy.</p>
    </div>
    <div class="hero-actions">
        <a asp-action="Create" class="btn primary">Add state</a>
        <a asp-controller="Home" asp-action="Index" class="btn ghost">Back to dashboard</a>
    </div>
</section>

<table class="modern-table">
    <thead>
        <tr>
            <th>State</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (!Model.Any())
        {
            <tr>
                <td colspan="3">
                    <div class="empty">No states yet. Add the first one.</div>
                </td>
            </tr>
        }
        else
        {
            foreach (var state in Model)
            {
                <tr>
                    <td>
                        <strong>@state.StateName</strong>
                    </td>
                    <td>
                        <form asp-action="Toggle"
                              asp-route-id="@state.StateId"
                              asp-route-activate="@(state.IsActive ? false : true)"
                              method="post"
                              class="toggle-switch">
                            <button type="submit"
                                    class="toggle-switch__button @(state.IsActive ? "is-active" : string.Empty)"
                                    aria-pressed="@(state.IsActive ? "true" : "false")">
                                <span class="toggle-switch__track">
                                    <span class="toggle-switch__thumb"></span>
                                </span>
                                <span class="toggle-switch__label">@(
                                        state.IsActive ? "Active" : "Inactive"
                                    )</span>
                            </button>
                        </form>
                    </td>
                    <td class="actions">
                        <a asp-action="Edit" asp-route-id="@state.StateId">Edit</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
